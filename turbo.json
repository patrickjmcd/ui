{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "outputs": [
        "dist/**/*",
        ".next/**/*"
      ],
      "dependsOn": [
        "^build"
      ]
    }
  }
}